<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 用户语音界面 -->
    <RelativeLayout
        android:id="@+id/voice_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#F5F5F5"
        android:visibility="visible">

        <!-- 语音输入按钮 -->
        <Button
            android:id="@+id/voice_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@drawable/btn_speak_material"
            android:text="按住说话"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:minWidth="320dp"
            android:minHeight="96dp"
            android:padding="20dp"
            android:elevation="8dp" />

        <!-- 底部状态提示 -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="32dp"
            android:text="欢迎使用 BlindAssist"
            android:textColor="#666666"
            android:textSize="16sp" />

    </RelativeLayout>

    <!-- 开发测试界面 -->
    <LinearLayout
        android:id="@+id/debug_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center_horizontal"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AutoGLM 通信测试"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="30dp"/>

        <EditText
            android:id="@+id/et_command"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="请输入指令，例如：打开设置"
            android:minHeight="48dp"
            android:layout_marginBottom="20dp"/>

        <Button
            android:id="@+id/btn_start_test"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="发送指令 (Start Task)"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/btn_stop_test"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="停止任务 (Stop)"
            android:backgroundTint="#FF5252"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="* 测试前请确保已开启无障碍服务"
            android:textColor="#888888"
            android:layout_marginTop="20dp"/>

    </LinearLayout>

</FrameLayout>
